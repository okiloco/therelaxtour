RewriteEngine On
# Redirect to https, but except for one domain...
RewriteCond %{SERVER_PORT} 80
RewriteCond %{HTTP_HOST} !^therelaxtour\.com.*
RewriteCond %{HTTP_HOST} !^www\.therelaxtour\.com.*
RewriteRule ^(.*)$ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]